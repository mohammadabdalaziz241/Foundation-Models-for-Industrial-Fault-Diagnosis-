# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7

# Machine learning
scikit-learn>=1.3

# Deep learning
torch>=2.1
torchvision>=0.16

# Data
requests>=2.31
tqdm>=4.66

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Testing
# Was missing from the environment: .venv had torch but no pytest, and the system
# interpreter had pytest but no torch, so `pytest tests/` could not run in either.
pytest>=7.4
